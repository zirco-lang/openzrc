# OpenZRC Build System

This is an open version of Zirco, meaning that it is completely community developed.
The original implementation of Zirco is written purely in Rust, which while a good language, makes it not so easy to compile on its own, using zirco.

This version should be developed in a "bootstrap" system, with a C subset being developed to allow for a purely zirco implementation to work.

